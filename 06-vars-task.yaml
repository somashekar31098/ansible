- name: variables 
  hosts: localhost
  tasks:
  - name: writing vars 
    vars:
    course: "devops"
    trainer: "soma"
    time: "15"
    ansible.builtin.debug:
     msg: "i am learning {{course}} from {{trainer}} for {{time}}"